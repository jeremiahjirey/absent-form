{
  "name": "student-attendance-lambda",
  "version": "1.0.0",
  "description": "AWS Lambda function for student attendance API",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "mysql2": "^3.14.0",
    "uuid": "^9.0.1"
  }
}
